  
#include<stdio.h>
	
int	main(){
	
		float area, lado;
		printf("Digite o valor do lado: ");
		scanf("%f", &lado);
		area=(lado*lado);
		printf("A area do quadrado e: %f",area);
		return 0;
}